-- Kept only as a compatibility notice. Run `npm run migrate` to apply the
-- ordered, transactional migrations in db/migrations.
SELECT 'Run npm run migrate before starting the production service' AS migration_notice;
